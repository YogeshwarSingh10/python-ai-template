[project]
name = "my-project"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "pydantic>=2.0",
    "python-dotenv",
    "pyyaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
src = ["src"]


[tool.setuptools.packages.find]
where = ["src"]
